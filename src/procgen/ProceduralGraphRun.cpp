#include "procgen/ProceduralGraph.hpp"  // brings Graph, Options, declarations, etc.

namespace pg {

void run_procedural_graph(Graph& g, const Options& opt) {
    // >>> move the function body here exactly as it was in the header <<<
}

} // namespace pg
