// src/map_viewer/ColonyMapViewerMain.cpp
// Entry point for the ColonyMapViewer executable.

#include <cstdlib>   // EXIT_SUCCESS / EXIT_FAILURE
// #include "MapViewerApp.h"   // <- whatever header defines your viewer app

int main(int /*argc*/, char** /*argv*/)
{
    // TODO: hook this into your real map-viewer application.
    // For now we just return success so the target links and runs.

    // Example of what you'd eventually want:
    /*
    MapViewerApp app;
    if (!app.Initialize())
        return EXIT_FAILURE;
    return app.Run();
    */

    return EXIT_SUCCESS;
}
