#pragma once
#include <vector>

// Fwds
struct World;
struct Colony;

// --- MOVE FROM GAME.CPP: hostiles (if any)
/*
struct Hostile {
    // pos, state, etc.
};
*/

// --- MOVE FROM GAME.CPP: per-agent + batch updates
/*
void hostileUpdate(Hostile& h, World& w, Colony& c, double dt);
void hostilesUpdate(std::vector<Hostile>& hs, World& w, Colony& c, double dt);
*/
