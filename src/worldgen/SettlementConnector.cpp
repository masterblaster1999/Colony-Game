#include "worldgen/SettlementConnector.hpp"
#include <limits>   // anything needed by the impl only
// #include <Windows.h>  // <- if *really* necessary, keep it in .cpp, not in the header

using namespace worldgen;

// Define everything here (A*, smoothing, MST glue, etc.)
