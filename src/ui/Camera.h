#pragma once
#include <cstdint>

struct Camera {
    // --- MOVE FROM GAME.CPP: your exact camera fields (center, zoom, shake, etc.)
    // int x, y; float zoom; ...
};

// --- MOVE FROM GAME.CPP: camera helpers (world<->screen conversions, clamp, etc.)
/*
void cameraInit(Camera& cam, int screenW, int screenH, int mapW, int mapH);
void cameraUpdate(Camera& cam, double dt);
*/
